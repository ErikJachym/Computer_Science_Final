class Cart{
  //What is the first part of every class? Type it below.
constructor(i,q){
  this.itemList = i;
  this.itemQuantity = q;
  this.days = d;
}


  //Type the instance functions below this comment.
addItem(i,q){
  itemList.push(i);
  itemQuantity.push(q);
}
totalCart(){
  var total= 0;

  for(let t=0; t<this.itemList.length;t++);
total+= (this.itemList[i].price*this.itemQuantity[q];
}
  return total;

  subCart(d){
  let subCart= new cart([],[]);

  for(let i=0;i<this.itemList.length;i)
    if this.itemList.length= d
}
}
